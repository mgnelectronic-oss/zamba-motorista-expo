/**
 * Rota = apenas `ride_live_route.polyline` (decode). Uma Polyline — valores fixos (incl. APK release).
 */

/** Azul estilo Google Maps Directions. */
export const ROUTE_LINE_COLOR = '#4285F4';
export const ROUTE_LINE_WIDTH = 5;
